twist_mux:
  ros__parameters:
    topics:
      teleop:
        topic   : cmd_vel
        timeout : 0.5
        priority: 5
      navigation:
        topic   : cmd_vel_nav
        timeout : 0.5
        priority: 10
      tracker:
        topic   : cmd_vel_tracker
        timeout : 0.5
        priority: 20
      joystick:
        topic   : cmd_vel_joy
        timeout : 0.5
        priority: 100